package com.hongyongfeng.neteasecloudmusic.util

import android.content.Context
import androidx.fragment.app.Fragment
import androidx.fragment.app.FragmentManager

class FragmentExistsUtil {
    fun isExists(mFragmentManager:FragmentManager,mContext:Context){
//        val fragments: List<Fragment> = mFragmentManager.fragments
//        for (fragment in fragments) {
////            Log.e(TAG, "navigate: " + fragment.getClass().getName());
//            ft.hide(fragment)
//        }
//        val tag: String = java.lang.String.valueOf(destination.getId())
//        var frag: Fragment? = mFragmentManager.findFragmentByTag(tag)
//        if (frag == null) {
//            frag = instantiateFragment(mContext, mFragmentManager, className, args)
//            if (frag != null) {
//                frag.setArguments(args)
//            }
//            ft.add(mContainerId, frag, tag)
//        }
//        ft.show(frag)
    }
}